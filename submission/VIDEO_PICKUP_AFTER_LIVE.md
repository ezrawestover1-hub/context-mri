# Optional post-live video pickup

Record this only after `npm run evidence:live` succeeds and the raw artifact is deployed. Replace the bracketed values with the actual published artifact values; do not claim them in advance.

> The public screen offers a deterministic replay, so anyone can inspect Context MRI without spending a credit. For this security-release case, I also ran a separate fresh GPT-five-point-six Sol audit. It includes the full ablation, a pre-registered policy-and-controls pair check, and pack verification: [trace count] timestamped traces moved the score from [baseline] to [optimized]. Every trace is inspectable, and the Context Guard still blocks the unsafe runbook.

Suggested placement: after the existing fixture/live explanation. Keep it to about 16 seconds. If fresh evidence is not published, do not add this pickup; the existing video is already accurate about the 21-trace public fixture replay.
