Here is the whole test. Give Context M, R, I the job, the files the agent reads, and a clear example of success. It runs the same task several times, changing only one file at a time. Everything else stays fixed, so the comparison is fair.
