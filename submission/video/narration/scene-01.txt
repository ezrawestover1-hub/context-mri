An A I agent can fail even when its model is good. Context M, R, I works like a medical scan for the instructions around that model. It finds the one file causing damage, measures the effect, and checks that removing it really helps.
